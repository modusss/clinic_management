<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Agendar Consulta</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#059669">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- Font Awesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" 
        integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" 
        crossorigin="anonymous" referrerpolicy="no-referrer" />
  
  <!-- Google Fonts - Inter for clean, modern typography -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    /* ============================================================================
     * Self-Booking Layout Styles
     * 
     * This is a mobile-first, clean design for patient self-scheduling.
     * The design prioritizes simplicity, accessibility, and ease of use.
     * ============================================================================ */
    
    * {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      -webkit-tap-highlight-color: transparent;
    }
    
    body {
      background: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 50%, #d1fae5 100%);
      min-height: 100vh;
      min-height: 100dvh;
    }
    
    /* Main container styling */
    .self-booking-container {
      max-width: 480px;
      margin: 0 auto;
      padding: 1rem;
      min-height: 100vh;
      min-height: 100dvh;
      display: flex;
      flex-direction: column;
    }
    
    /* Card styling for content areas */
    .booking-card {
      background: white;
      border-radius: 1.5rem;
      box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.05), 0 2px 4px -2px rgb(0 0 0 / 0.05);
      padding: 1.5rem;
      margin-bottom: 1rem;
    }
    
    /* Primary action button */
    .btn-primary {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 1rem 1.5rem;
      background: linear-gradient(135deg, #059669 0%, #047857 100%);
      color: white;
      font-weight: 600;
      font-size: 1.125rem;
      border-radius: 1rem;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      box-shadow: 0 4px 14px -3px rgba(5, 150, 105, 0.4);
    }
    
    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 20px -3px rgba(5, 150, 105, 0.5);
    }
    
    .btn-primary:active {
      transform: translateY(0);
    }
    
    /* Secondary/outline button */
    .btn-secondary {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 1rem 1.5rem;
      background: white;
      color: #059669;
      font-weight: 600;
      font-size: 1rem;
      border-radius: 1rem;
      border: 2px solid #059669;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
    }
    
    .btn-secondary:hover {
      background: #f0fdf4;
    }
    
    /* Selection option buttons */
    .option-btn {
      display: flex;
      align-items: center;
      justify-content: space-between;
      width: 100%;
      padding: 1.25rem 1.5rem;
      background: white;
      color: #374151;
      font-weight: 500;
      font-size: 1.125rem;
      border-radius: 1rem;
      border: 2px solid #e5e7eb;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      margin-bottom: 0.75rem;
    }
    
    .option-btn:hover {
      border-color: #059669;
      background: #f0fdf4;
    }
    
    .option-btn:active {
      transform: scale(0.98);
    }
    
    .option-btn .icon {
      color: #9ca3af;
      transition: color 0.2s ease;
    }
    
    .option-btn:hover .icon {
      color: #059669;
    }
    
    /* Greeting text styling */
    .greeting-text {
      font-size: 1.75rem;
      font-weight: 700;
      color: #111827;
      line-height: 1.3;
    }
    
    .patient-name {
      color: #059669;
    }
    
    /* Step indicator */
    .step-indicator {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
    }
    
    .step-dot {
      width: 0.5rem;
      height: 0.5rem;
      border-radius: 50%;
      background: #d1d5db;
      transition: all 0.3s ease;
    }
    
    .step-dot.active {
      background: #059669;
      width: 1.5rem;
      border-radius: 0.25rem;
    }
    
    .step-dot.completed {
      background: #10b981;
    }
    
    /* Alert/notice styling */
    .alert {
      padding: 1rem;
      border-radius: 0.75rem;
      margin-bottom: 1rem;
    }
    
    .alert-error {
      background: #fef2f2;
      border: 1px solid #fee2e2;
      color: #991b1b;
    }
    
    .alert-success {
      background: #f0fdf4;
      border: 1px solid #dcfce7;
      color: #166534;
    }
    
    /* Success checkmark animation */
    @keyframes checkmark-pop {
      0% { transform: scale(0); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }
    
    .success-icon {
      animation: checkmark-pop 0.5s ease-out;
    }
    
    /* Period selection cards */
    .period-card {
      padding: 1.5rem;
      border-radius: 1rem;
      border: 2px solid #e5e7eb;
      cursor: pointer;
      transition: all 0.2s ease;
      text-align: center;
    }
    
    .period-card:hover {
      border-color: #059669;
      background: #f0fdf4;
    }
    
    .period-card .icon {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
    }
    
    .period-morning .icon { color: #f59e0b; }
    .period-afternoon .icon { color: #3b82f6; }
    
    /* Back button */
    .back-btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: #6b7280;
      font-weight: 500;
      text-decoration: none;
      padding: 0.5rem 0;
      margin-bottom: 1rem;
    }
    
    .back-btn:hover {
      color: #059669;
    }
    
    /* ============================================================================
     * Patient Selection Cards
     * Used when multiple patients are associated with the same phone number.
     * Similar style to day selection cards for visual consistency.
     * ============================================================================ */
    .patient-card {
      display: flex;
      align-items: center;
      gap: 1rem;
      width: 100%;
      padding: 1rem 1.25rem;
      background: white;
      border-radius: 1rem;
      border: 2px solid #e5e7eb;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      text-align: left;
    }
    
    .patient-card:hover {
      border-color: #059669;
      background: #f0fdf4;
      transform: translateX(4px);
    }
    
    .patient-card:active {
      transform: scale(0.98);
    }
    
    .patient-card-avatar {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 3rem;
      height: 3rem;
      background: linear-gradient(135deg, #e9d5ff 0%, #d8b4fe 100%);
      color: #7c3aed;
      font-weight: 700;
      font-size: 1.25rem;
      border-radius: 50%;
      flex-shrink: 0;
    }
    
    .patient-card-info {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 0.125rem;
    }
    
    .patient-card-name {
      font-weight: 600;
      font-size: 1.125rem;
      color: #1f2937;
    }
    
    .patient-card-fullname {
      font-size: 0.875rem;
      color: #6b7280;
    }
    
    .patient-card:hover .patient-card-avatar {
      background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
      color: #059669;
    }
    
    .patient-card:hover .fa-chevron-right {
      color: #059669;
      transform: translateX(2px);
    }
    
    .patient-card .fa-chevron-right {
      transition: all 0.2s ease;
    }
    
    /* Outline secondary button (subtle, less prominent) */
    .btn-outline-secondary {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 0.875rem 1.5rem;
      background: transparent;
      color: #6b7280;
      font-weight: 500;
      font-size: 0.9375rem;
      border-radius: 1rem;
      border: 1.5px dashed #d1d5db;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
    }
    
    .btn-outline-secondary:hover {
      background: #f9fafb;
      border-color: #9ca3af;
      color: #4b5563;
    }
    
    /* ============================================================================
     * Existing Appointment Card
     * Displays when patient already has a future appointment scheduled.
     * Provides clear visibility and option to keep or reschedule.
     * ============================================================================ */
    .existing-appointment-card {
      background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%);
      border: 2px solid #10b981;
      border-radius: 1.5rem;
      padding: 1.25rem;
      margin-bottom: 1rem;
      box-shadow: 0 4px 14px -3px rgba(16, 185, 129, 0.2);
    }
    
    .existing-appointment-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 1rem;
    }
    
    .existing-appointment-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 3rem;
      height: 3rem;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      font-size: 1.25rem;
      border-radius: 50%;
      flex-shrink: 0;
    }
    
    .existing-appointment-title {
      font-weight: 700;
      font-size: 1.125rem;
      color: #065f46;
      margin: 0;
    }
    
    .existing-appointment-subtitle {
      font-size: 0.875rem;
      color: #047857;
      margin: 0;
    }
    
    .existing-appointment-details {
      background: white;
      border-radius: 1rem;
      padding: 1rem;
      margin-bottom: 1rem;
    }
    
    .existing-appointment-date,
    .existing-appointment-time,
    .existing-appointment-referral {
      display: flex;
      align-items: center;
      font-size: 0.9375rem;
      color: #374151;
      margin-bottom: 0.5rem;
    }
    
    .existing-appointment-date:last-child,
    .existing-appointment-time:last-child,
    .existing-appointment-referral:last-child {
      margin-bottom: 0;
    }
    
    .existing-appointment-date {
      font-weight: 600;
      text-transform: capitalize;
    }
    
    .existing-appointment-actions {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    
    .btn-keep-appointment {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 0.875rem 1.5rem;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      font-weight: 600;
      font-size: 1rem;
      border-radius: 0.75rem;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      box-shadow: 0 4px 14px -3px rgba(16, 185, 129, 0.4);
    }
    
    .btn-keep-appointment:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 20px -3px rgba(16, 185, 129, 0.5);
    }
    
    /* Reschedule divider */
    .reschedule-divider {
      display: flex;
      align-items: center;
      margin: 1rem 0;
    }
    
    .reschedule-divider::before,
    .reschedule-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, #d1d5db, transparent);
    }
    
    .reschedule-divider-text {
      padding: 0 1rem;
      font-size: 0.8125rem;
      color: #9ca3af;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    /* ============================================================================
     * Phone Changed Alert
     * Shown when user changes phone in the change_name form
     * ============================================================================ */
    .phone-changed-alert {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border: 1px solid #f59e0b;
      border-radius: 1rem;
      padding: 1rem;
      margin-bottom: 1rem;
    }
    
    .phone-changed-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 2.5rem;
      height: 2.5rem;
      background: #f59e0b;
      color: white;
      border-radius: 50%;
      flex-shrink: 0;
    }
    
    .phone-changed-content {
      flex: 1;
    }
    
    .phone-changed-title {
      font-weight: 600;
      font-size: 0.9375rem;
      color: #92400e;
      margin: 0 0 0.25rem 0;
    }
    
    .phone-changed-text {
      font-size: 0.8125rem;
      color: #a16207;
      margin: 0;
      line-height: 1.4;
    }
    
    /* WhatsApp button */
    .btn-whatsapp {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 1rem 1.5rem;
      background: linear-gradient(135deg, #25d366 0%, #128c7e 100%);
      color: white;
      font-weight: 600;
      font-size: 1.0625rem;
      border-radius: 1rem;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      box-shadow: 0 4px 14px -3px rgba(37, 211, 102, 0.4);
    }
    
    .btn-whatsapp:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 20px -3px rgba(37, 211, 102, 0.5);
    }
    
    .btn-whatsapp:active {
      transform: translateY(0);
    }
    
    /* Link Sent Confirmation */
    .link-sent-confirmation {
      text-align: center;
      padding: 1.5rem 1rem;
    }
    
    .link-sent-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 4rem;
      height: 4rem;
      background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
      color: #059669;
      font-size: 2rem;
      border-radius: 50%;
      margin-bottom: 1rem;
      animation: checkmark-pop 0.5s ease-out;
    }
    
    .link-sent-title {
      font-weight: 700;
      font-size: 1.25rem;
      color: #065f46;
      margin: 0 0 0.5rem 0;
    }
    
    .link-sent-text {
      font-size: 0.9375rem;
      color: #374151;
      margin: 0 0 1rem 0;
      line-height: 1.5;
    }
    
    .link-sent-instruction {
      font-size: 0.8125rem;
      color: #6b7280;
      margin: 0;
      padding: 0.75rem;
      background: #f3f4f6;
      border-radius: 0.75rem;
    }
    
    /* Phone Fixed Display (for new_registration) */
    .phone-fixed-display {
      display: flex;
      align-items: center;
      width: 100%;
      padding: 0.875rem 1rem;
      background: #f0fdf4;
      border: 2px solid #10b981;
      border-radius: 0.75rem;
      font-size: 1.125rem;
      font-weight: 500;
      color: #065f46;
    }
    
    /* Existing Profile Notice */
    .existing-profile-notice {
      display: flex;
      align-items: flex-start;
      gap: 0.5rem;
      padding: 0.875rem 1rem;
      background: #eff6ff;
      border: 1px solid #bfdbfe;
      border-radius: 0.75rem;
      font-size: 0.875rem;
      color: #1e40af;
      line-height: 1.5;
    }
    
    .existing-profile-notice i {
      margin-top: 0.125rem;
      flex-shrink: 0;
    }
    
    /* Hidden utility class */
    .hidden {
      display: none !important;
    }
    
    /* ============================================================================
     * Share Section Styles
     * Used on the success page to share booking link with friends
     * ============================================================================ */
    .share-section {
      margin-top: 2rem;
      padding-top: 1.5rem;
    }
    
    .share-divider {
      display: flex;
      align-items: center;
      margin-bottom: 1rem;
    }
    
    .share-divider::before,
    .share-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, #d1d5db, transparent);
    }
    
    .share-divider span {
      padding: 0 1rem;
      font-size: 0.875rem;
      color: #6b7280;
      font-weight: 500;
    }
    
    .btn-share-whatsapp {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 1rem 1.5rem;
      background: linear-gradient(135deg, #25d366 0%, #128c7e 100%);
      color: white;
      font-weight: 600;
      font-size: 1.0625rem;
      border-radius: 1rem;
      border: none;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
      box-shadow: 0 4px 14px -3px rgba(37, 211, 102, 0.4);
    }
    
    .btn-share-whatsapp:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px -3px rgba(37, 211, 102, 0.5);
    }
    
    .btn-share-whatsapp:active {
      transform: translateY(0);
    }
    
    .btn-share-whatsapp i {
      font-size: 1.25rem;
    }
    
    /* Responsive adjustments */
    @media (max-height: 700px) {
      .booking-card {
        padding: 1rem;
      }
      
      .greeting-text {
        font-size: 1.5rem;
      }
      
      .patient-card {
        padding: 0.875rem 1rem;
      }
      
      .patient-card-avatar {
        width: 2.5rem;
        height: 2.5rem;
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="self-booking-container">
    <% if flash[:alert].present? %>
      <div class="alert alert-error">
        <i class="fas fa-exclamation-circle mr-2"></i>
        <%= flash[:alert] %>
      </div>
    <% end %>
    
    <% if flash[:notice].present? %>
      <div class="alert alert-success">
        <i class="fas fa-check-circle mr-2"></i>
        <%= flash[:notice] %>
      </div>
    <% end %>
    
    <%= yield %>
  </div>
</body>
</html>
