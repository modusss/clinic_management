<%= breadcrumb({title: "Todos os atendimentos", path: services_path}, {title: "Voltar ao atendimento", path: service_path(@service)}, {title: "Novo encaixe", path: nil}) %>

<div class="container mt-6">
  <h1 class="text-2xl font-bold mb-4" style="margin: 10px">Encaixar novo paciente para <%= @service.date.strftime("%d/%m") %></h1>
  <%= render "form_patient_fitted", invitation: @invitation,
            referrals: @referrals,
            service: @service,
            referral: "",
            date: "" %>

  <br>

</div>
