<div class="text-lg font-medium text-green-700 mb-4">
  <%= notice %>
</div>
<% unless referral?(current_user) %>
<%= breadcrumb({title: "Pacientes", path: leads_path}, {title: @lead.name, path: ""}) %>
<% end %>

<div class="bg-white shadow overflow-hidden sm:rounded-lg mb-6">
  <%= render @lead %>
</div>

<div class="flex items-center space-x-4">
  <%= link_to "Editar dados do responsável", edit_lead_path(@lead), class: "text-white bg-indigo-500 hover:bg-indigo-600 px-4 py-2 rounded" %>
  <% unless referral?(current_user) %>
  <%= link_to "Votlar para pacientes", leads_path, class: "text-indigo-500 hover:text-indigo-600" %>
  <%= button_to "Excluir esta ficha", @lead, method: :delete, class: "text-white bg-red-500 hover:bg-red-600 px-4 py-2 rounded" %>
  <% end %>
</div>
