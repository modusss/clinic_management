<div class="mt-6 max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
  <h1 class="text-3xl font-bold text-center">Editar paciente</h1>
  <%= render "form", lead: @lead %>
</div>

<div class="mt-6 max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 flex justify-center space-x-4">
  <%= link_to "Dados deste paciente", @lead, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %> 
  <%= link_to "Voltar para pacientes", leads_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %> 
</div>
