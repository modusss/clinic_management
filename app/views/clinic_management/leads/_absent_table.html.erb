<%= data_table(rows, 2) %>

<% if leads.size > 10 %>
  <%= paginate leads %>
<% end %>

<%= turbo_frame_tag "message_sent_notification" %>

<style>
@media (max-width: 768px) {

  .pagination {
    flex-direction: column;
  }

  .pagination li {
    margin: 10px;
  }
}
</style>