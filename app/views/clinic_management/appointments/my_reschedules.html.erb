<%= breadcrumb({title: "Meus Agendamentos", path: nil}) %>

<%# Toggle for table/cards view. Stores preference in a cookie. %>
<%= render partial: "clinic_management/shared/toggle_cards_table", locals: { 
      path: ->(args = {}) { url_for(params.permit!.merge(args)) }, 
      cookie_name: "preferred_reschedules_view", 
      view_type: @view_type || 'table'
    } %>

<div class="container py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-2 px-4">Meus Agendamentos</h1>
    <p class="text-gray-600 px-4">Lista de agendamentos realizados por você.</p>
  </div>

  <!-- Filtros de Conversão (Comprou/Não Comprou) -->
  <div class="px-4 mb-4">
    <div class="flex items-center gap-3">
      <span class="text-sm font-medium text-gray-700">Conversão:</span>
      
      <%= link_to my_reschedules_appointments_path(filter: @filter),
                  class: "inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{@conversion_filter.nil? ? 'bg-gray-700 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'}" do %>
        Todos
      <% end %>
      
      <%= link_to my_reschedules_appointments_path(filter: @filter, conversion_filter: 'purchased'),
                  class: "inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{@conversion_filter == 'purchased' ? 'bg-green-600 text-white' : 'bg-green-50 text-green-700 hover:bg-green-100'}" do %>
        <svg class="w-4 h-4 mr-1.5" fill="currentColor" viewBox="0 0 20 20">
          <path d="M3 1a1 1 0 000 2h1.22l.305 1.222a.997.997 0 00.01.042l1.358 5.43-.893.892C3.74 11.846 4.632 14 6.414 14H15a1 1 0 000-2H6.414l1-1H14a1 1 0 00.894-.553l3-6A1 1 0 0017 3H6.28l-.31-1.243A1 1 0 005 1H3zM16 16.5a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0zM6.5 18a1.5 1.5 0 100-3 1.5 1.5 0 000 3z"></path>
        </svg>
        Compraram
        <span class="ml-1.5 px-2 py-0.5 rounded-full text-xs bg-green-200 text-green-800">
          <%= @purchased_count %>
        </span>
      <% end %>
      
      <%= link_to my_reschedules_appointments_path(filter: @filter, conversion_filter: 'not_purchased'),
                  class: "inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{@conversion_filter == 'not_purchased' ? 'bg-red-600 text-white' : 'bg-red-50 text-red-700 hover:bg-red-100'}" do %>
        <svg class="w-4 h-4 mr-1.5" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M13.477 14.89A6 6 0 015.11 6.524l8.367 8.368zm1.414-1.414L6.524 5.11a6 6 0 018.367 8.367zM18 10a8 8 0 11-16 0 8 8 0 0116 0z" clip-rule="evenodd"></path>
        </svg>
        Não Compraram
        <span class="ml-1.5 px-2 py-0.5 rounded-full text-xs bg-red-200 text-red-800">
          <%= @not_purchased_count %>
        </span>
      <% end %>
    </div>
  </div>

  <!-- Abas de Filtro de Origem -->
  <div class="px-4 mb-6">
    <div class="border-b border-gray-200">
      <nav class="-mb-px flex space-x-8" aria-label="Tabs">
        <!-- Aba: Todas -->
        <%= link_to my_reschedules_appointments_path(filter: 'all', conversion_filter: @conversion_filter), 
                    class: "whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors #{@filter == 'all' ? 'border-blue-500 text-blue-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'}" do %>
          Todas
          <span class="ml-2 py-0.5 px-2.5 rounded-full text-xs font-medium <%= @filter == 'all' ? 'bg-blue-100 text-blue-600' : 'bg-gray-100 text-gray-900' %>">
            <%= @total_count %>
          </span>
        <% end %>
        
        <!-- Aba: Orgânicas -->
        <%= link_to my_reschedules_appointments_path(filter: 'organic', conversion_filter: @conversion_filter), 
                    class: "whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors #{@filter == 'organic' ? 'border-green-500 text-green-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'}" do %>
          <span class="flex items-center">
            <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            Orgânicas
          </span>
          <span class="ml-2 py-0.5 px-2.5 rounded-full text-xs font-medium <%= @filter == 'organic' ? 'bg-green-100 text-green-600' : 'bg-gray-100 text-gray-900' %>">
            <%= @organic_count %>
          </span>
        <% end %>
        
        <!-- Aba: Esforço Pessoal -->
        <%= link_to my_reschedules_appointments_path(filter: 'active_effort', conversion_filter: @conversion_filter), 
                    class: "whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors #{@filter == 'active_effort' ? 'border-blue-500 text-blue-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'}" do %>
          <span class="flex items-center">
            <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9 2a1 1 0 000 2h2a1 1 0 100-2H9z"></path>
              <path fill-rule="evenodd" d="M4 5a2 2 0 012-2 3 3 0 003 3h2a3 3 0 003-3 2 2 0 012 2v11a2 2 0 01-2 2H6a2 2 0 01-2-2V5zm3 4a1 1 0 000 2h.01a1 1 0 100-2H7zm3 0a1 1 0 000 2h3a1 1 0 100-2h-3zm-3 4a1 1 0 100 2h.01a1 1 0 100-2H7zm3 0a1 1 0 100 2h3a1 1 0 100-2h-3z" clip-rule="evenodd"></path>
            </svg>
            Esforço Pessoal
          </span>
          <span class="ml-2 py-0.5 px-2.5 rounded-full text-xs font-medium <%= @filter == 'active_effort' ? 'bg-blue-100 text-blue-600' : 'bg-gray-100 text-gray-900' %>">
            <%= @active_effort_count %>
          </span>
        <% end %>
        
        <!-- Aba: Sem Origem (antigas) -->
        <% if @undefined_count > 0 %>
          <%= link_to my_reschedules_appointments_path(filter: 'undefined', conversion_filter: @conversion_filter), 
                      class: "whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors #{@filter == 'undefined' ? 'border-gray-500 text-gray-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'}" do %>
            <span class="flex items-center">
              <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
              </svg>
              Sem Origem
            </span>
            <span class="ml-2 py-0.5 px-2.5 rounded-full text-xs font-medium <%= @filter == 'undefined' ? 'bg-gray-200 text-gray-700' : 'bg-gray-100 text-gray-900' %>">
              <%= @undefined_count %>
            </span>
          <% end %>
        <% end %>
      </nav>
    </div>
  </div>

  <!-- Informação do filtro ativo -->
  <div class="px-4 mb-4">
    <p class="text-sm text-gray-600">
      <% case @filter %>
      <% when 'organic' %>
        Mostrando <span class="font-semibold text-green-600"><%= @appointments.total_count %></span> agendamentos orgânicos
      <% when 'active_effort' %>
        Mostrando <span class="font-semibold text-blue-600"><%= @appointments.total_count %></span> agendamentos por esforço pessoal
      <% when 'undefined' %>
        Mostrando <span class="font-semibold text-gray-600"><%= @appointments.total_count %></span> agendamentos sem origem definida
      <% else %>
        Mostrando <span class="font-semibold"><%= @appointments.total_count %></span> agendamentos no total
      <% end %>
    </p>
  </div>

  <%# Render table or cards view based on @view_type %>
  <% if @rows.present? %>
    <% if (@view_type || 'table') == 'cards' %>
      <%= render partial: "reschedule_cards", locals: { rows: @rows } %>
    <% else %>
      <div data-controller="table" class="overflow-hidden border-gray-200 sm:rounded-lg">
          <%= data_table(@rows) %>
          <br>
      </div>
    <% end %>
  <% else %>
    <div class="bg-white shadow sm:rounded-lg">
      <div class="px-4 py-5 sm:p-6">
        <h2 class="text-xl font-medium text-gray-900">Nenhum agendamento encontrado.</h2>
        <p class="mt-2 text-sm text-gray-600">Você ainda não fez nenhum agendamento.</p>
      </div>
    </div>
  <% end %>
  
  <% if @appointments.present? %>
    <div class="mt-6">
      <%= paginate @appointments %>
    </div>
  <% end %>
  
  <br>
  <br>
  <br>
  <br>
</div>

<style>
/* Abas responsivas */
nav[aria-label="Tabs"] {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
}

nav[aria-label="Tabs"]::-webkit-scrollbar {
    display: none;
}

@media screen and (max-width: 767px) {
    .patient-name.nowrap{
        white-space: normal;
    }
    
    .comments {
        white-space: normal;
        max-width: 200px;
        word-wrap: break-word;
    }
    
    /* Abas em mobile */
    nav[aria-label="Tabs"] {
        flex-wrap: nowrap;
    }
    
    nav[aria-label="Tabs"] a {
        flex-shrink: 0;
    }
}

.reschedule-comments {
    max-width: 300px;
}

.reschedule-comments textarea {
    min-height: 60px;
    font-size: 14px;
}

.reschedule-comments .mt-2 {
    font-size: 11px;
}

/* Estilo para links do WhatsApp */
.fa-whatsapp {
    color: #25D366;
}

/* Melhorar aparência dos links */
a.text-blue-500:hover {
    text-decoration: underline;
}
</style>
