<div class="flex flex-col">
  <%= link_to service.date.strftime("%d/%m/%Y"), 
      clinic_management.service_path(service), 
      class: "text-blue-500 hover:text-blue-700 nowrap font-medium", 
      target: "_blank" %>
  
  <%# Indicador de tipo de agendamento %>
  <% 
    # Encontra o appointment associado a este serviço na lista sendo renderizada
    # Como estamos dentro de uma partial que recebe 'service', precisamos encontrar o appointment correto
    # O appointment específico não é passado diretamente para esta partial na iteração atual do controller,
    # mas podemos inferir ou ajustar o controller para passar o objeto appointment.
    # Porem, a partial é renderizada com locals: { service: service } no controller.
    # Vamos ajustar o controller para passar 'appointment' também para esta partial.
    
    current_appointment = if local_assigns[:appointment]
                            appointment
                          else
                            # Fallback caso appointment não seja passado (mas vamos corrigir no controller)
                            service.appointments.order(created_at: :desc).first
                          end
                          
    is_reschedule = if current_appointment
                      ClinicManagement::Appointment.where(lead_id: current_appointment.lead_id)
                                                   .where('created_at < ?', current_appointment.created_at)
                                                   .where(status: 'remarcado')
                                                   .exists?
                    else
                      false
                    end
  %>
  
  <% if is_reschedule %>
    <span class="inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-purple-100 text-purple-800 mt-1 w-fit" title="Este paciente já teve um agendamento anterior remarcado">
      <svg class="mr-1 h-3 w-3" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M4 2a1 1 0 011 1v2.101a7.002 7.002 0 0111.601 2.566 1 1 0 11-1.885.666A5.002 5.002 0 005.999 7H9a1 1 0 010 2H4a1 1 0 01-1-1V3a1 1 0 011-1zm.008 9.057a1 1 0 011.276.61A5.002 5.002 0 0014.001 13H11a1 1 0 110-2h5a1 1 0 011 1v5a1 1 0 11-2 0v-2.101a7.002 7.002 0 01-11.601-2.566 1 1 0 01.61-1.276z" clip-rule="evenodd"/>
      </svg>
      Reagendado
    </span>
  <% else %>
    <span class="inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-800 mt-1 w-fit" title="Novo agendamento (encaixe)">
      <svg class="mr-1 h-3 w-3" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-11a1 1 0 10-2 0v2H7a1 1 0 100 2h2v2a1 1 0 102 0v-2h2a1 1 0 100-2h-2V7z" clip-rule="evenodd"/>
      </svg>
      Encaixado
    </span>
  <% end %>
</div>
